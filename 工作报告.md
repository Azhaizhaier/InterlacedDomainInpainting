# 硕士学位论文工作进展报告

## 一、研究内容简介

### 1.1 选题背景

裸眼 3D 显示是近年显示技术的重要发展方向之一。其中，柱状透镜（Lenticular Lens）方案通过将多视点图像按子像素级别交织到同一显示面板，使观看者无需佩戴辅助设备即可获得立体视觉。实际应用中，受限于渲染资源和实时性要求，系统通常只渲染一个源视点，再借助深度信息将单视角图像 warp 到多个目标视角。由于遮挡关系的存在，warp 过程会在目标视图中产生大量空洞，经过柱状透镜交织后，这些空洞会进一步分散为显示域中不规则分布的子像素级缺失区域，直接降低立体显示质量。

传统处理思路通常是在 view domain 中先补全目标视图，再执行交织映射。然而，从 8 个完整 RGB 视图交织成 3 通道 interlaced 图像是一个有损采样过程，每个显示像素只保留每个视图的一个子像素，其余颜色通道信息在交织时已经丢失，因此完整恢复原始视图属于病态问题。另一方面，显示域的交织映射是确定且可计算的：每个子像素由哪个视图提供，完全由显示参数和透镜 LUT 决定。这种确定性的显示映射关系构成了一种可利用的结构先验。

### 1.2 研究目标

本课题的目标是直接在裸眼 3D 显示的 interlaced domain 恢复缺失子像素，而不是先补全完整视图再交织。具体包括：

1. 建立 interlaced-domain 空洞填补任务的训练数据生成、验收和评估流程；
2. 分析柱状透镜 LUT 的周期结构与显示映射先验；
3. 设计能够利用显示映射先验的网络输入、特征聚合方式和损失约束；
4. 通过规则方法、通用图像修复模型和 U-Net 等基线，系统评估所提方法的有效性；
5. 最终形成可复现的算法、评估指标与实验结论，为裸眼 3D 显示系统提供显示域修复方案。

### 1.3 拟开展的主要研究内容

本课题拟围绕“显示映射先验驱动的 interlaced-domain 空洞填补”展开以下研究：

1. 数据生成与质量验证：基于单视角 RGB + Depth 生成多视角图像，完成柱状透镜交织，生成 interlaced input、interlaced mask 和 interlaced GT，并建立文件完整性、mask 结构、有效区 PSNR 等多维度验收标准。
2. 显示映射先验分析：实现与显示硬件一致的 LUT 生成，验证 `(1,6)` 等精确周期，量化各 view 在显示像素中的覆盖密度，研究将 view 归属信息转化为网络输入与特征聚合约束的方法。
3. 网络模型设计：从普通 U-Net 基线出发，逐步引入 view-ID 编码、LUT 引导的 neighbor aggregation、view-plane 一致性损失，以及可选的显式 view-plane 表示，形成模块化方法。
4. 损失函数与评估协议：以 masked inpainting 为主任务，采用全图与空洞区域加权损失；评估 interlaced-domain PSNR/SSIM，以及反交织后的 per-view 采样位置指标。
5. 实验与消融：在规则法、LaMa、U-Net 等基线上，依次验证 view-ID、LUT neighbor aggregation、view-plane 损失等模块的贡献，完成跨场景泛化分析。

## 二、研究工作进展

截至目前，本课题已完成数据管线、显示映射分析、基线实验和第一轮结构化输入实验，具体进展如下。

### 2.1 数据管线与样本验收（M1）

已完成 Python 项目骨架、LUT 工具和样本加载模块。核心实现包括：

- `build_lut`：按显示参数生成 4K 子像素级 view LUT，支持全局坐标 `origin`，保证随机裁剪后仍能对齐完整显示映射。
- de-interlace / re-interlace：实现 interlaced 图像到稀疏 per-view 平面的双向重排，验证了该过程在同一 LUT 下精确可逆。
- 周期分析：验证当前显示参数下 LUT 的精确周期为 `(1,6)`；每个显示像素恰好包含 3 个不同视图的子像素；每个视图的像素覆盖率约为 32%-43%。
- 样本验收：实现 `validate` 命令行，检查文件完整性、mask 二值性、`mask_view` 一致性，并以“有效区 PSNR >= 30 dB”作为数据质量门槛。
- hole-aware 采样：实现随机裁剪采样器，约 50% 优先裁剪包含空洞的区域，避免大量无洞 crop 导致的训练退化。

已对 `E:\dataset_out` 中的现有样本完成统计：当前约 67 个样本，scene001 为 12 个样本；scene001 中 interp 平均 hole ratio 约 4.44%，extrap 约 6.88%。

### 2.2 基线实验（M2）

完成了 B0 规则法、B1 LaMa 和 B2 U-Net 三条基线：

- B0：最近邻、水平插值、双向插值、形态学、region growing，均按子像素 mask 逐通道处理。
- B1：接入 `simple_lama_inpainting`，作为通用图像修复强基线。
- B2：U-Net，输入为 RGB + 子像素 mask 共 6 通道，使用 masked inpainting 监督：`pred = input * (1-mask) + network_output * mask`。

训练环境为 RTX 4080 SUPER，B2 使用 256x256 crop、1000 步、batch 8、base 32。同时实现了 4K 全图 tiled 推理，tile 256、overlap 32，支持边缘无死循环和重叠区线性融合。

scene001/sample_0000 全图评估结果如下：

| 方法 | full PSNR | hole PSNR | SSIM |
| --- | --- | --- | --- |
| nearest | 21.76 | 5.70 | 0.8963 |
| horizontal | 26.81 | 11.01 | 0.9448 |
| bidirectional | 27.25 | 11.49 | 0.9477 |
| morphology | 28.03 | 12.36 | 0.9511 |
| lama | 27.10 | 11.32 | 0.9460 |
| unet_tiled_baseline | 32.07 | 17.28 | 0.9636 |

结果显示，通用 U-Net 在全图和空洞区域均明显优于规则法与 LaMa，证明了 interlaced-domain 学习式修复路线可行。

### 2.3 显示映射先验实验（M3）

在 B2 基础上实现了两种显示映射先验输入：

- P1：view-ID 编码。将 LUT 中每个子像素所属视图归一化为额外输入通道，输入由 6 通道增至 9 通道。
- P2：LUT-guided neighbor aggregation。按同 view 周期格点 `(1,6)` 及其倍数聚合邻域颜色与有效性，作为附加输入特征。

P1/P2 使用与 B2 相同的训练配置。全图评估结果如下：

| 方法 | full PSNR | hole PSNR | SSIM | view PSNR |
| --- | --- | --- | --- | --- |
| unet_tiled_baseline | 32.07 | 17.28 | 0.9636 | 31.81 |
| unet_tiled_view_id | 31.36 | 16.34 | 0.9609 | 31.21 |
| unet_tiled_neighbor | 30.74 | 15.55 | 0.9613 | 30.58 |

当前第一轮结果表明，view-ID 编码和当前形式的 LUT 邻居特征在 scene001/sample_0000 上尚未带来提升，甚至略低于 B2。需要结合数据规模扩大、训练稳定性和特征聚合方式进一步分析。

### 2.4 评估指标

已实现并冻结 view-aware 评估指标：

- 将预测结果与 `interlaced_gt` 按 LUT 反交织为稀疏 per-view 平面；
- 计算每个 view 在采样位置上的 PSNR/L1；
- 报告全部 view 的平均值；source view 对应平面无空洞时 PSNR 为 inf。

该指标不要求恢复完整 RGB view，只对显示域已观测子像素按视图分组，符合 interlacing 不可逆、但 view evidence 可精确提取的任务特点。

### 2.5 已取得的成果小结

1. 建立了完整的 interlaced-domain 数据生成、验收、训练、推理和评估代码链路。
2. 验证了显示映射周期结构，并把 LUT 先验以 view-ID 和 neighbor aggregation 形式接入网络。
3. 完成 B0/B1/B2/P1/P2 五类方法的全图对比，证明 U-Net baseline 已超过规则法和 LaMa。
4. 完成 per-view 指标冻结与实现，为后续 view-plane 损失和跨视图分析提供了基础。
5. 所有单元测试在 conda 环境中通过，当前测试规模为 18 项。

## 三、目前存在或预期可能出现的问题

### 3.1 数据规模不足

当前实验主要基于 scene001 的 12 个样本，样本量小且场景单一。虽然随机裁剪能生成大量 crop，但同一场景的纹理、深度和空洞结构高度相关，模型容易过拟合，无法可靠判断 P1/P2 等方法的真实贡献。

### 3.2 P1/P2 暂未带来提升

第一轮实验中，view-ID 与 LUT neighbor aggregation 均未超过 B2 baseline。可能原因包括：

- 训练样本太少，方法差异被过拟合噪声掩盖；
- 当前 neighbor aggregation 只是输入特征拼接，网络可能没有有效利用；
- 只评估一个样本，结果受单样本偶然性影响；
- 需要引入 view-plane 损失或可学习聚合层，才能充分发挥显示映射先验。

### 3.3 有效子像素与 GT 不完全一致

`interlaced_input` 来自整数像素 forward warp，`interlaced_gt` 来自 Blender 对目标相机的重新渲染，两者在非空洞位置也存在 ±1 px 的取整差异。当前任务按 masked inpainting 处理，以“保留输入有效像素 + 填补空洞”为主，GT 用于评估和诊断，但这也意味着模型无法通过保留有效像素达到完全对齐 GT 的上限。

### 3.4 评估口径与泛化

目前主要使用 scene001/sample_0000 单样本全图评估，B2 的训练损失统计使用 crop 级评估。正式结论需要：

- 建立场景级 Train/Val/Test 划分；
- 多样本、多场景统计；
- 多随机种子重复实验；
- 全图推理与 crop 评估口径统一。

### 3.5 其他潜在问题

- 4K 全图推理依赖 tiled 方案，tile 边缘融合仍需在更多样本上验证。
- LaMa 依赖外部模型包，需要固定版本以保证可复现性。
- depth 信息尚未接入网络，后续如果使用立体一致性损失，需要解决单通道 view 平面无法直接做 RGB 级比较的问题。
- 显示参数变化会改变 LUT 周期，当前实现已支持动态生成，但正式实验仍应固定显示参数或记录参数卡片。

## 四、下一步的工作计划

### 4.1 数据集扩样与划分

按照 `DATA_GENERATION_PLAN.md` 生成 300+ 样本：

- 200 view 场景：scene001、scene004、scene005 等，采用 `6 sources x 4 steps x 2 modes = 48 samples/scene`；
- 短视角场景：scene002、scene003、scene006 按视角数调整 source/step；
- 对 scene001/scene005 增加 focus 变体；
- 生成后执行 M1 验收，统计各场景 hole ratio 与有效区 PSNR；
- 建立 `split.json`，按场景划分 Train/Val/Test，避免同场景跨集合。

### 4.2 重新训练与消融

使用扩充后的数据集重新训练 B2/P1/P2：

- 统一 256 或 512 crop、1000+ 步、多个随机种子；
- 在 Train/Val/Test 全样本上统计 mean +/- std；
- 将 P2 从输入特征拼接升级为可学习 LUT-guided aggregation 层，或与 Interlace-aligned Conv 对比；
- 完成 view-ID、neighbor aggregation、训练时长、采样策略等消融。

### 4.3 引入 view-plane 一致性损失

实现 P3：

- 在 U-Net 上增加稀疏 view-plane head；
- 按 LUT 反交织后在 view 采样位置计算 L1/TV；
- 与仅使用 RGB 损失对比，验证是否降低跨 view 串扰；
- 在 view PSNR、view L1 和全图 PSNR 上联合报告。

### 4.4 深度先验与立体一致性

在 P3 稳定后，将源 depth 交织为 interlaced depth 输入，并评估使用 depth 与 baseline 的立体一致性损失。该部分作为扩展实验，不进入第一阶段主线。

### 4.5 正式评估与论文写作

完成全场景全图评估，统一指标口径，补充可视化和主观评估方案，整理消融表格，形成硕士学位论文初稿。
